import { Text } from "react-native";
import { AppScreen } from "@/components/layout/AppScreen";

export default function Home() {
  return (
    <AppScreen>
      <Text>Home (Member)</Text>
    </AppScreen>
  );
}
